<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Question Details</title>

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
        font-family: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
      }

      body {
        background: linear-gradient(to right, #6a11cb, #2575fc);
        color: #fff;
        min-height: 100vh;
        display: flex;
        flex-direction: column;
      }

      .container {
        width: 90%;
        max-width: 900px;
        margin: 60px auto;
      }

      .question-card {
        background-color: rgba(255, 255, 255, 0.1);
        border-radius: 18px;
        padding: 35px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.35);
      }

      .question-title {
        font-size: 2.2rem;
        margin-bottom: 15px;
      }

      .question-meta {
        font-size: 0.95rem;
        color: #e0e0e0;
        margin-bottom: 10px;
      }

      .question-image img {
        width: 100%;
        max-height: 400px;
        object-fit: cover;
        border-radius: 14px;
        margin: 25px 0;
      }

      .question-description {
        font-size: 1.1rem;
        line-height: 1.7;
        white-space: pre-line;
        margin-bottom: 25px;
      }

      .answer-card {
        background-color: rgba(255, 255, 255, 0.1);
        padding: 25px;
        border-radius: 16px;
        margin-top: 30px;
      }

      .answerofthequestions {
        margin-bottom: 20px;
      }

      textarea {
        width: 100%;
        min-height: 120px;
        padding: 12px;
        border-radius: 12px;
        border: none;
        margin-bottom: 15px;
        background-color: rgba(255, 255, 255, 0.2);
        color: #fff;
      }

      button {
        padding: 12px 25px;
        background: linear-gradient(135deg, #667eea, #764ba2);
        border: none;
        border-radius: 25px;
        color: #fff;
        font-weight: bold;
        cursor: pointer;
      }

      .actions {
        margin-top: 30px;
      }

      .btn-back {
        background: #ffeb3b;
        color: #000;
        padding: 10px 20px;
        border-radius: 25px;
        text-decoration: none;
        font-weight: bold;
      }

      footer {
        margin-top: auto;
        padding: 20px;
        text-align: center;
        background: rgba(0, 0, 0, 0.6);
      }
    </style>
  </head>

  <body>
    <!-- Navbar -->
    <%- include("../components/navbar") %>

    <div class="container">
      <div class="question-card">
        <!-- Question -->
        <div class="question-title"><%= question.title %></div>

        <div class="question-meta">
          Asked by <strong><%= question.user.username %></strong>
        </div>
        <div class="question-meta"><%= question.timeAgo %></div>

        <% if (question.image) { %>
        <div class="question-image">
          <img src="/storage/<%= question.image %>" alt="Question Image" />
        </div>
        <% } %>

        <div class="question-description"><%= question.description %></div>

        <!-- Answers -->
        <div class="answer-card">
          <h3>Answers</h3>

          <% if (answerData.length === 0) { %>
          <p>No answers yet. Be the first to answer!</p>
          <% } %> <% answerData.forEach(answer => { %>
          <div class="answerofthequestions">
            <p><%= answer.answerText %></p>
            <p>— <strong>Author:<%= answer.user.username %></strong></p>
            <p>Post on: <%= answer.timeAgo %></p>
            <!-- like dislikes -->
            <div class="answerofthequestions">
              <form
                action="/answer/1/react"
                method="POST"
                style="display: inline"
              >
                <input type="hidden" name="type" value="like" />
                <button type="submit">👍 10</button>
              </form>

              <form
                action="/answer/1/react"
                method="POST"
                style="display: inline"
              >
                <input type="hidden" name="type" value="dislike" />
                <button type="submit">👎 2</button>
              </form>

              <hr style="opacity: 0.3" />
            </div>
            <hr style="opacity: 0.3" />
          </div>
          <% }) %>
        </div>

        <!-- Answer Form -->
        <div class="answer-card">
          <h3>Write Your Answer</h3>

          <form action="/answer/<%= question.id %>" method="POST">
            <textarea
              name="answer"
              placeholder="Type your answer here..."
              required
            ></textarea>
            <button type="submit">Post Answer</button>
          </form>
        </div>

        <!-- Back -->
        <div class="actions">
          <a href="/" class="btn-back">← Back to Questions</a>
        </div>
      </div>
    </div>

    <footer>© 2025 Asmit Khanal. All rights reserved.</footer>
  </body>
</html>
